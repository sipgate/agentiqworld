# Project Environment Variables
# Copy this file to .env and fill in your values
#
# Usage: cp .env.example .env

# =============================================================================
# Node.js
# =============================================================================
NODE_ENV=development

# =============================================================================
# Server Ports
# =============================================================================
# Port offset - shift all ports by this amount to avoid conflicts
# Example: PORT_OFFSET=1000 gives you ports 4000, 4001, 4002, 9090
PORT_OFFSET=0

# Individual ports (calculated from offset, or set manually)
HOMEPAGE_PORT=3000
WEBAPP_PORT=3001
ADMIN_PORT=3002
POCKETBASE_PORT=8090

# Path prefix for webapp when mounted on homepage (set WEBAPP_PORT empty to enable)
WEBAPP_PATH=/

# Path prefix for admin when mounted on homepage (set ADMIN_PORT empty to enable)
ADMIN_PATH=/admin

# =============================================================================
# PocketBase
# =============================================================================
# API URL for client-side JavaScript (update if using PORT_OFFSET)
API_URL=http://localhost:8090

# =============================================================================
# Anthropic API (for AI agent)
# =============================================================================
ANTHROPIC_API_KEY=sk-ant-your-key-here
GEMINI_API_KEY=AIza-your-key-here

# =============================================================================
# MCP Server Credentials (add to ~/.zshrc, not here!)
# =============================================================================
# These environment variables are used by Claude Code's MCP integrations.
# Add them to your shell profile (~/.zshrc or ~/.bashrc), NOT this file.
# After adding, run: source ~/.zshrc
#
# REQUIRED - PocketBase MCP (database operations)
# Create these when you first access http://localhost:8090/_/
# export POCKETBASE_ADMIN_EMAIL="admin@example.com"
# export POCKETBASE_ADMIN_PASSWORD="your-password"
#
# REQUIRED - GitHub MCP (repo management)
# Create at: https://github.com/settings/tokens (scopes: repo, read:org)
# export GITHUB_PERSONAL_ACCESS_TOKEN="ghp_xxx"
